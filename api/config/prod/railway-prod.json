{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS",
		"buildCommand": "poetry install"
	},
	"deploy": {
		"startCommand": "python manage.py collectstatic --noinput && gunicorn app.wsgi",
		"restartPolicyType": "NEVER",
		"restartPolicyMaxRetries": 10
	}
}
