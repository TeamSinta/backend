{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS",
		"buildCommand": "poetry install"
	},
	"deploy": {
		"startCommand": "python manage.py makemigrations company && python manage.py makemigrations user && python manage.py makemigrations && python manage.py migrate && python manage.py initsite && python manage.py initsocial_app && python manage.py collectstatic --noinput && daphne -p $PORT app.asgi:application",
		"restartPolicyType": "NEVER",
		"restartPolicyMaxRetries": 10
	}
}
