{
  "$schema": "https://schema.up.railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["...", "ffmpeg"],
          "cmds": [
            "...",
            "sudo add-apt-repository ppa:mc3man/trusty-media && sudo apt-get update && sudo apt-get install ffmpeg"
          ]
        }
      }
    },
    "buildCommand": "poetry install"
  },
  "deploy": {
    "startCommand": "python manage.py makemigrations company && python manage.py makemigrations user && python manage.py makemigrations && python manage.py migrate && python manage.py initsite && python manage.py initsocial_app && python manage.py collectstatic --noinput && daphne -p $PORT -b 0.0.0.0 app.asgi:application",
    "restartPolicyType": "NEVER",
    "restartPolicyMaxRetries": 10
  }
}
